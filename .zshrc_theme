autoload -Uz colors && colors

autoload -Uz vcs_info
zstyle ':vcs_info:*' check-for-changes true
# zstyle ':vcs_info:git*' actionformats "%b %m%u%c"
# zstyle ':vcs_info:git*' formats "%b %a %m%u%c"
zstyle ':vcs_info:*' enable git svn
zstyle ':vcs_info:*' max-exports 3
zstyle ':vcs_info:*' formats '%b %a %m%u%c' '%s' '%S'


# COLOURS
RVM_COLOUR="%F{magenta}%}"
DIR_COLOUR="%B%F{blue}%}"
GIT_COLOUR="%B%F{black}% "

# ELEMENTS
DIR_="$DIR_COLOUR%~ "
[[ "$SPIN" ]] && LOC_="🌀" || LOC_="🏠"


theme_precmd() {
  vcs_info
}

autoload -Uz add-zsh-hook
add-zsh-hook precmd theme_precmd

PROMPT='$DIR_$reset_color$GIT_COLOUR${vcs_info_msg_0_} $reset_color% $LOC_'